import { Meta } from '@storybook/addon-docs/blocks'
import { ThemeTokens } from './ThemeTokens'

<Meta title="Token/Theme" />

# Theme

Semantic tokens that map to the palette per theme — surface, border, text, and the
primary/success/danger/warning roles. Use the toolbar's theme toggle (top of the Storybook UI)
to switch this page between light and dark; defined in `src/styles/tokens.css`.

<ThemeTokens />
